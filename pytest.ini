[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    playwright: tests that launch a real Chromium browser (require playwright install chromium)
    integration: tests that hit real external APIs
